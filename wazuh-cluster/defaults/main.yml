---
# defaults file for oci-rsa-ansible-wazuh
elasticsearch_security_user: admin
elasticsearch_security_password: changeme
wazuh_api_users:
  - username: "wazuh"
    password: "wazuh"

wazuh_node_type: worker

# Override with a 32-character key, using the same key through the cluster
wazuh_cluster_key: 1111222233334444aaaabbbbcccdddd